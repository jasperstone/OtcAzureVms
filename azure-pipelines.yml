trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  - template: Templates/vars.yml

jobs:
  - job:
    variables:
      VMNAME: 'USS-SRV-11'
    steps:
    - template: Templates/vm-creation-steps.yml
  - job:
    variables:
      VMNAME: 'USS-SRV-12'
    steps:
    - template: Templates/vm-creation-steps.yml
  - job:
    variables:
      VMNAME: 'USS-SRV-13'
    steps:
    - template: Templates/vm-creation-steps.yml